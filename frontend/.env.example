# Proxy mode, used by the default /api/backend frontend client.
CLI_AGENT_BACKEND_URL=http://127.0.0.1:8765
# CLI_AGENT_BACKEND_BEARER_TOKEN=
# CLI_AGENT_BACKEND_NGROK=false

# Browser-direct mode. Leave unset unless the browser should call the Python API directly.
# NEXT_PUBLIC_BACKEND_URL=http://localhost:8765
